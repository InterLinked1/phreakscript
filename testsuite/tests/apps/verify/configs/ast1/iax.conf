[general]
bindport=4569
bindaddr=127.0.0.1
disallow=all
allow=ulaw
jitterbuffer=no
forcejitterbuffer=no
encryption=yes

[localhost]
type = friend
context = from-localhost
username = localhost
auth = md5
secret = mysecret
